# Netlify Redirects - SPA Configuration
# DO NOT modify unless you know what you're doing

# 1. Proxy API calls to localhost backend during local dev
/api/*  http://localhost:8000/api/:splat  200

# 2. SPA fallback - must be LAST
/*      /index.html   200

